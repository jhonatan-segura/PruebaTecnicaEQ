import React from "react";
import UploadForm from "./components/UploadForm";

const App: React.FC = () => {
  return (
    <div style={{width: "100vw"}}>
      <UploadForm />
    </div>
  );
};

export default App;
